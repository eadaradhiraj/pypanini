# Progress — Done / Next (overwritten each iteration, not appended)

Date: 2026-09-06T00:00:00Z
Sweep: 482/1166 100%

## Done
- Pure generative Satf `urv`-coda lengthening (`tUrv->tUrvan` not `torvan`, consonant-initial shape; vowel-initial `urv` keeps guna; gate is case-insensitive because krdanta pre-maps `ur->Ur`).
- 7-member `urv` set improved again (`01.0651-0657`, e.g. `01.0651 871->877` +6 Satf). Re-sweep: `482/1166` held, 7 improved, 0 worsened (`krut -21`, `yangluk -21`). Pilots OK. (`BU` Satf untouched: `Bavan`.)

## Next
1. Batch next (`urv` remainders: liw `totUrva`, luN `atUrvIt`; then `01.0038`, `01.0049`).
2. Re-sweep, rebuild STATS, overwrite this, commit & push.
