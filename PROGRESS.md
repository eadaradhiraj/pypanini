# Progress — Done / Next (overwritten each iteration, not appended)

Date: 2026-09-06T00:00:00Z
Sweep: 482/1166 100%

## Done
- Pure generative ASIrliN `urv`-coda lengthening (`turv->tUrvyAt`, mUla parasmaipadi stem shape; `lw` keeps short-`u` untouched).
- Full 8-member `urv` set improved incl. `purva` (`01.0650-0657`, e.g. `01.0651 862->871` +9 ASIrliN). Re-sweep: `482/1166` held, 8 improved, 0 worsened (`ting -5`). Pilots OK.

## Next
1. Batch next (`urv` remainders: liw `totUrva`, luN `atUrvIt`, Satf/tumun; then `01.0038`, `01.0049`).
2. Re-sweep, rebuild STATS, overwrite this, commit & push.
