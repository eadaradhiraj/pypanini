# Progress — Done / Next (overwritten each iteration, not appended)

Date: 2026-09-05T00:00:00Z
Sweep: 343/1166 100%

## Done
- Atman `liw` e-redup + final-cons (`cak→ceke`, single-cons no-r, general): `01.0098 99%→100%` + ~11 similar (`sasace` etc.).
- Re-sweep: `331 → 343/1166` (+12). Pilots OK. No hardcoding.

## Next
1. `yang_krut/yat` (`cAcAyyaH` vs `cekIyyaH`, 99.7%, y-final yang).
2. `krut` remainder + `ting` leftovers.
3. Re-sweep, rebuild STATS, overwrite this, commit & push.
